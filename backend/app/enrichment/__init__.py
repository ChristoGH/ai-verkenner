"""Enrichment (PLACEHOLDER).

Task 005 adds LLM enrichment: classification, the five scores (hype inverted), summary,
why-it-matters, connection-to-work, and a recommended action. It MUST import the priority rule
from app.scoring.priority — it must never re-implement it. Nothing calls an LLM yet.
"""
