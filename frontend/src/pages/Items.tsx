export function Items() {
  return (
    <div className="space-y-2">
      <h1 className="text-2xl font-semibold">Items</h1>
      <p className="text-sm text-muted-foreground">
        The full, filterable enriched feed. Wired to GET /items in Task 006.
      </p>
    </div>
  );
}
